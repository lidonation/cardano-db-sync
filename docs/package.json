{
  "name": "cardano-db-sync-docs",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler d1 execute dbsync --file=schema.sql --local && astro dev",
    "build": "wrangler d1 execute dbsync --file=schema.sql --remote && astro build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist --project-name cardano-db-sync-docs",
    "db:apply": "wrangler d1 execute dbsync --file=schema.sql --remote",
    "db:apply:local": "wrangler d1 execute dbsync --file=schema.sql --local"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "@astrojs/mdx": "^5.0.5",
    "astro": "^6.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "typescript": "^5.4.0",
    "wrangler": "^4.83.0"
  }
}
